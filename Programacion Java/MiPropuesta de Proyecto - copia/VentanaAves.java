import javax.swing.*;
//import java.awt.*;

public class VentanaAves extends JFrame {

    public VentanaAves() {
        super("Aves"); // Título de la ventana
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);

        // Configurar el contenido de la ventana de aves

        pack();
        setLocationRelativeTo(null);
        setVisible(true);
    }

    // Otros métodos y componentes específicos de la ventana de aves
}
